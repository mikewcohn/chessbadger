---
import BaseLayout from '../../layouts/BaseLayout.astro'
import PuzzleCollectionProgress from '../../components/PuzzleCollectionProgress'
import { steps2Workbook } from '../../lib/puzzleCollections'
---

<BaseLayout title="Steps 2 Workbook | ChessBadger" description="Track progress through the Steps 2 Workbook puzzles">
  <section class="bg-[#eef6ff]">
    <div class="mx-auto max-w-7xl px-5 py-8 sm:px-6 sm:py-11">
      <p class="text-sm font-bold tracking-[0.04em] text-amber-800">
        <a href="/puzzles" class="hover:text-amber-700">Puzzles</a>
        <span class="mx-2 text-stone-400" aria-hidden="true">/</span>
        Collection
      </p>
      <h1 class="mt-3 max-w-4xl text-4xl font-bold leading-[1.02] tracking-[-0.025em] text-stone-950 sm:text-6xl">
        {steps2Workbook.title}
      </h1>
      <p class="mt-4 max-w-2xl text-lg text-stone-600">{steps2Workbook.description}</p>
    </div>
  </section>

  <section class="bg-stone-100">
    <div class="mx-auto max-w-7xl px-5 py-8 sm:px-6 sm:py-10">
      <PuzzleCollectionProgress collection={steps2Workbook} client:load />
    </div>
  </section>
</BaseLayout>
